# Use the latest version of CodeQL
version: latest

# Specify the language and directory containing the source code
languages:
  - go
  source_directories:
    - .

# Run the custom CodeQL rule on the Go code
queries:
  - query: ./rules/
